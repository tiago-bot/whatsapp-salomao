{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -c \"import os; port = os.environ.get('PORT', '8080'); import subprocess; subprocess.run(['uvicorn', 'main_hubspot:app', '--host', '0.0.0.0', '--port', port])\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
